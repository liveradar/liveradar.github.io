{
  "name": "liveradar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal live-music event radar. Static frontend + manually-triggered Node scraper pipeline, no build step (SPEC D14).",
  "scripts": {
    "fetch": "node scripts/fetch.mjs",
    "serve": "node scripts/dev-server.mjs",
    "test": "node --test scripts/*.test.mjs src/*.test.js"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "js-yaml": "^5.4.2",
    "playwright": "^1.63.0"
  }
}
